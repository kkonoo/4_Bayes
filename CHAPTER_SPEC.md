# 베이즈 강의 챕터 작성 규격 (반드시 준수)

대상: 생명정보학 랩 대학원생. R 경험 있음, 빈도주의 통계 경험 있음, 베이지안은 처음.
언어: **한국어**. 전문용어는 첫 등장 시 "한국어(English)" 병기.
문체: 평서문 "~합니다" 체. 과장·이모지 금지. 설명은 구체적으로, 군더더기 없이.

## 1. 파일 형식

Quarto `.qmd`. YAML front matter는 아래 틀을 그대로 따릅니다.

```
---
title: "W{N}. {챕터 제목}"
description: "{한 줄 설명}"
author: "Eunji Ha"
date: today

format:
  html:
    include-in-header:
      text: |
        <meta name="ai-title" content="W{N}. {챕터 제목}">
        <meta name="ai-context" content="
        {이 챕터의 핵심 개념을 2~3문단으로 요약. AI 학습도우미의 시스템 프롬프트에
        주입되는 내용이므로, 정의·기호·핵심 공식·실습에서 쓴 함수 이름을 구체적으로 적을 것.
        큰따옴표(") 를 절대 쓰지 말 것 — HTML 속성이 깨집니다. 작은따옴표만 사용.}
        ">
---
```

## 2. 본문 구조 (순서 고정)

1. `## 학습 목표` — 불릿 3~5개. "~을 설명할 수 있다 / 구현할 수 있다" 형태.
2. 도입 1~2문단: 이번 주 문제를 **생명정보학 맥락의 구체적 상황**으로 연다.
3. 개념 섹션 여러 개 (`##`). 각 섹션은 **직관 → 작은 수치 예 → 수식** 순서.
4. 수식은 반드시 접힌 블록 안에 둔다:
   ```
   ::: {.callout-note collapse="true"}
   ## 수식으로 보기
   $$ ... $$
   기호 설명
   :::
   ```
   본문에 인라인 수식($p(\theta)$ 정도)은 허용. 긴 유도는 전부 접은 블록으로.
5. `::: {.callout-tip}` = 핵심 직관 한 덩어리. `::: {.callout-warning}` = 흔한 오해/함정.
6. `## 실습: base R로 직접 구현` — 알고리즘을 손으로 구현.
7. (해당 시) `## 실습: 패키지로 풀기` — 같은 문제를 실제 패키지로.
8. `## 연습문제` — 3문제. 각 문제 바로 아래에
   `::: {.callout-note collapse="true"}` / `## 풀이` / 코드+설명 / `:::`
9. `## 정리` — 불릿 4~6개.
10. `## 다음 시간` — 다음 주 내용 2~3줄 예고.

## 3. R 코드 규칙 (엄격)

- 사이트는 `eval: true`로 렌더됩니다. **모든 `{r}` 청크는 실제로 실행되어야 합니다.**
- 사용 가능 패키지: **base R + stats + graphics 뿐** (CI에 tidyverse도 있지만 쓰지 않습니다).
  `rstan`, `brms`, `loo`, `posterior`, `bayesplot` 등은 CI에 없으므로,
  해당 청크에는 반드시 `#| eval: false` 를 첫 줄에 붙이고,
  그 결과를 보여줘야 하면 base R로 동등한 계산을 따로 수행해 그림을 그립니다.
- **외부 데이터 다운로드·파일 읽기 금지.** 데이터는 코드로 생성(`rbinom`, `rnorm` 등)하거나
  R 내장 데이터셋(`sleepstudy` 같은 패키지 데이터는 불가; `mtcars`, `ToothGrowth` 등만)을 씁니다.
- 난수를 쓰면 `set.seed(42)` 등 시드를 반드시 고정.
- 청크 하나의 실행 시간은 수 초 이내. MCMC는 반복 2,000~10,000회 수준으로.
- 그림은 base graphics(`plot`, `hist`, `curve`, `lines`, `matplot`, `image`).
  주색 `"#1a6e3c"`, 대비색 `"#c0392b"`, 보조 `"#2c7fb8"`, 회색 `"grey60"`.
  모든 그림에 `main`, `xlab`, `ylab` 지정. 필요하면 `legend()`.
  여러 패널은 `par(mfrow=...)` 사용 후 원복 불필요(청크 종료 시 초기화됨).
- 코드에는 한국어 주석을 충분히 답니다.
- 변수명은 영어 소문자 스네이크케이스.

## 4. 분량 및 링크

- 250~400줄.
- 다른 주차 참조는 상대 링크: `[W2](02_conjugate.qmd)` 형태(같은 chapters/ 폴더).
- 파일 맨 끝에 빈 줄 하나.

## 5. 금지 사항

- ai-context 안에 큰따옴표 사용 금지.
- `library(tidyverse)`, `%>%`, `ggplot2` 사용 금지.
- 존재하지 않는 함수·인자 사용 금지. 확신이 없으면 더 단순한 base R로 작성.
- 출처 없는 수치 인용 금지. 예시 데이터는 "가상의 데이터"임을 명시.
